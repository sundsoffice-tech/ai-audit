"""ai_audit.kms — Concrete KeyProvider implementations for external KMS systems.

Each submodule has its own optional dependency:
    ai_audit.kms.vault   requires: hvac
    ai_audit.kms.aws     requires: boto3
"""
